import type { AtlasMetadata } from "@/generated/models";

export function MethodsSection({ metadata }: { metadata: AtlasMetadata }) {
  return <section className="methods-section section" id="methods"><div className="section-heading"><div><span className="eyebrow">Data and interpretation</span><h2>Evidence with its limits attached</h2><p>Every displayed score retains its source vintage, geography, methodology version, and limitations.</p></div><span className="version-stamp">Generated {new Date(metadata.generated_at).toLocaleDateString()} · Loaded {new Date(metadata.loaded_at).toLocaleDateString()}</span></div><div className="source-grid">{metadata.sources.map((source, index) => <a className="source-card" key={source.key} href={source.url} target="_blank" rel="noreferrer"><span>0{index + 1}</span><h3>{source.label}</h3><strong>{source.vintage}</strong><p>{source.note}</p><small>Open source ↗</small></a>)}</div><div className="guardrail-grid"><Guardrail icon="!" title="Not diagnosis">County patterns never determine an individual patient’s diagnosis or safety.</Guardrail><Guardrail icon="↗" title="Not exposure location">Human surveillance is organized by residence, not confirmed exposure.</Guardrail><Guardrail icon="∅" title="Missing is not zero">No record can reflect sampling, testing, reporting, suppression, or publication limits.</Guardrail><Guardrail icon="≈" title="Different vintages">Inputs are public snapshots with different release dates, not real-time surveillance.</Guardrail></div><div className="mvp-boundary"><div><span className="eyebrow">MVP boundary</span><h3>Useful for prioritizing questions—not answering them alone.</h3></div><div className="boundary-columns"><div><strong>Included now</strong><ul><li>Human surveillance floor</li><li>Tick and pathogen status</li><li>SVI, uninsured percentile, and rurality</li></ul></div><div><strong>Deferred</strong><ul><li>Land cover and habitat suitability</li><li>State and local validation layers</li><li>Provider and laboratory capacity</li></ul></div></div></div><p className="limitations"><strong>Release limitation:</strong> {metadata.limitations}</p></section>;
}

function Guardrail({ icon, title, children }: { icon: string; title: string; children: React.ReactNode }) { return <article><span className="guardrail-icon">{icon}</span><h3>{title}</h3><p>{children}</p></article>; }
