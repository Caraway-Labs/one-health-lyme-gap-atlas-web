import type { AtlasMetadata } from "@/generated/models";
import type { EvidenceView } from "@/lib/atlas-ui";

export function AtlasFilters({ metadata, stateFilter, query, evidence, onStateChange, onQueryChange, onEvidenceChange, onDownload }: { metadata: AtlasMetadata; stateFilter: string; query: string; evidence: EvidenceView; onStateChange: (value: string) => void; onQueryChange: (value: string) => void; onEvidenceChange: (value: EvidenceView) => void; onDownload: () => void }) {
  return <div className="filter-bar"><label><span>State</span><select aria-label="State" value={stateFilter} onChange={(event) => onStateChange(event.target.value)}><option value="ALL">All states</option>{metadata.states.map((state) => <option value={state.code} key={state.code}>{state.name}</option>)}</select></label><label><span>County or FIPS</span><input aria-label="County or FIPS" value={query} onChange={(event) => onQueryChange(event.target.value)} placeholder="Search counties…" /></label><label><span>Evidence view</span><select aria-label="Evidence view" value={evidence} onChange={(event) => onEvidenceChange(event.target.value as EvidenceView)}><option value="all">All scorable counties</option><option value="ecological">Ecological evidence present</option><option value="human">Published human signal present</option><option value="complete">Five to six inputs available</option></select></label><button className="button export" onClick={onDownload}>Download ranking CSV</button></div>;
}
